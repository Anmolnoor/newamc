import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Buttons = () => {
  return (
    <View>
      <Text>This is the Buttons component for the Profile Screen.</Text>
    </View>
  );
};

export default Buttons;

const styles = StyleSheet.create({});
