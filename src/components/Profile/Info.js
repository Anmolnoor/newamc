import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Info = () => {
  return (
    <View>
      <Text>This is the Info Component for Profile Screen</Text>
    </View>
  );
};

export default Info;

const styles = StyleSheet.create({});
